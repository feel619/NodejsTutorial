# What is NPM ?

npm stands for Node Package Manager

It allows us to install libraries and tools made by other developers
```
npm install <package>
```

To initialize npm in your project, run:

```
npm init -y
```

This Create a package.json file, Which keeps track of your project and dependencies.

